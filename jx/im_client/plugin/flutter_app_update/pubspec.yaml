name: flutter_app_update
description: A new flutter plugin project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.jxim.flutter_app_update
        pluginClass: FlutterAppUpdatePlugin
      ios:
        pluginClass: FlutterAppUpdatePlugin
